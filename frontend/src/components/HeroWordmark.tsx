export default function HeroWordmark() {
  return (
    <div className="hero-wordmark mx-auto mb-5 w-full max-w-[720px] text-center">
      <p
        className="text-[clamp(14px,2.4vw,20px)] font-semibold tracking-[0.3em] uppercase text-gray-400 dark:text-gray-500 mb-3"
        aria-hidden="true"
      >
        Everything
      </p>
      <h1 className="text-[clamp(48px,9vw,96px)] font-black leading-[0.95] tracking-tight text-gray-900 dark:text-white">
        AI Coding
      </h1>
    </div>
  )
}
